<Window x:Class="TodoWpfApp.CalendarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TodoWpfApp"
        Title="Calendar" Height="600" Width="900">
    <Window.Resources>
        <local:TasksForDateConverter x:Key="TasksForDateConverter" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Calendar x:Name="TaskCalendar"
                  Margin="10"
                  SelectionMode="SingleDate"
                  SelectedDatesChanged="TaskCalendar_SelectedDatesChanged">
            <Calendar.Resources>
                <Style TargetType="CalendarDayButton">
                    <Setter Property="AllowDrop" Value="True" />
                    <EventSetter Event="Drop" Handler="DayButton_Drop" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="CalendarDayButton">
                                <Border x:Name="Root" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="2" CornerRadius="4">
                                    <StackPanel>
                                        <ContentPresenter HorizontalAlignment="Right" />
                                        <ItemsControl Margin="0,2,0,0">
                                            <ItemsControl.ItemsSource>
                                                <MultiBinding Converter="{StaticResource TasksForDateConverter}">
                                                    <Binding Path="DataContext" RelativeSource="{RelativeSource TemplatedParent}" />
                                                    <Binding Path="Tasks" RelativeSource="{RelativeSource AncestorType=Window}" />
                                                </MultiBinding>
                                            </ItemsControl.ItemsSource>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="#FFE8F4FF" Padding="2" Margin="0,0,0,2" CornerRadius="3">
                                                        <TextBlock Text="{Binding DisplayText}" FontSize="11" TextWrapping="Wrap" MouseMove="TaskEntry_MouseMove" />
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Calendar.Resources>
        </Calendar>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Tasks on selected date" FontWeight="Bold" FontSize="14" />
            <TextBlock Text="{Binding SelectedDateText}" Margin="0,2,0,8" />
            <ListBox ItemsSource="{Binding SelectedDateEntries}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#FFCCCCCC" BorderThickness="1" Padding="6" Margin="0,0,0,8" CornerRadius="3">
                            <StackPanel>
                                <TextBlock Text="{Binding DisplayText}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Details}" FontSize="12" Foreground="#FF555555" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <TextBlock Text="Tip: drag a task from a day cell onto another date to reschedule." FontSize="12" Foreground="#FF555555" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</Window>
