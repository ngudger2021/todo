<Window x:Class="TodoWpfApp.NotificationCenterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TodoWpfApp"
        Title="Notification Center" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        Icon="/ChatGPT Image Jan 29, 2026, 02_10_29 PM.png">
    <Window.Resources>
        <local:ReadToBrushConverter x:Key="ReadToBrushConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource SecondaryBackgroundBrush}" Padding="10">
            <StackPanel>
                <TextBlock Text="Notifications" FontSize="18" FontWeight="Bold"/>
                <TextBlock x:Name="CountText" Text="0 unread notifications" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <ListBox x:Name="NotificationsList" Grid.Row="1" Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10" BorderBrush="LightGray" BorderThickness="0,0,0,1" Background="{Binding Read, Converter={StaticResource ReadToBrushConverter}}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding TaskTitle}" FontWeight="Bold"/>
                            <TextBlock Grid.Row="1" Text="{Binding Message}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="2" Text="{Binding CreatedAt, StringFormat='Created: {0:g}'}" FontSize="10" Foreground="Gray" Margin="0,5,0,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Mark as Read" Width="100" Margin="5" Click="MarkAsRead_Click"/>
            <Button Content="Open Task" Width="100" Margin="5" Click="OpenTask_Click"/>
            <Button Content="Clear All" Width="100" Margin="5" Click="ClearAll_Click"/>
        </StackPanel>
    </Grid>
</Window>
